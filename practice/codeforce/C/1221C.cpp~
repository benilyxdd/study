#include <bits/stdc++.h>
using namespace std;

#define ll long long

void solve() {
	int c, m, x; cin >> c >> m >> x;
	int mn = min(c, m);
	int sum = c+m+x;
	int max_team = sum/3;
	cout << min(max_team, mn) << "\n";
}

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int t = 1, i = 1;
	cin >> t;
	while(t--) {
		//cout << "Case #" << i << ": ";
		solve();
		//i++;
	}
	return 0;
}


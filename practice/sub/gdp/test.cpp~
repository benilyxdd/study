#include <bits/stdc++.h>
using namespace std;

#define ll long long

class coin {
	public:
	int a, b;
	coin(int a, int b) : a(a), b(b){}
};

void solve() {
	int x, y;
	cin >> x >> y;
	coin a(x, y);
	cout << a.a << ' ' << a.b << '\n';
}

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int t = 1, i = 1;
	//cin >> t;
	while(t--) {
		//cout << "Case #" << i << ": ";
		solve();
		//i++;
	}
	return 0;
}


#include <bits/stdc++.h>
using namespace std;

#define ll long long

const int N = 10000
int prime[N];


void solve() {
	ll p, q; cin >> p >> q;
	if (p%q != 0 || p < q) {
		cout << p << "\n";
	} else {
		
	}
}

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int t = 1, i = 1;
	cin >> t;
	while(t--) {
		//cout << "Case #" << i << ": ";
		solve();
		//i++;
	}
	return 0;
}


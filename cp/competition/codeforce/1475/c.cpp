#include <bits/stdc++.h>
using namespace std;

#define ll long long

void solve() {
	int a, b, k;
	cin >> a >> b >> k;
	int ar[k], br[k];
	for (int i = 0; i < k; i++)
		cin >> ar[i];
	for (int i = 0; i < k; i++)
		cin >> br[i];


}

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int t = 1, i = 1;
	cin >> t;
	while(t--) {
		//cout << "Case #" << i << ": ";
		solve();
		//i++;
	}
	return 0;
}


#include <bits/stdc++.h>
using namespace std;

#define ll long long
const int mxN = (int)200001;
int n, cnt[400001];
array<int, 3> ar[mxN];

void solve() {
	cin >> n;
	for (int i = 0; i < n*2; i++) {
		
	}
}

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int t = 1, i = 1;
	//cin >> t;
	while(t--) {
		//cout << "Case #" << i << ": ";
		solve();
		//i++;
	}
	return 0;
}


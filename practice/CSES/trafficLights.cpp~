#include <bits/stdc++.h>
using namespace std;

#define ll long long
const int mxX = (int)1e9+9, mxN = (int)2e5+5;
int x, n, ar[mxN];

void solve() {
	cin >> x >> n;
	for (int i = 0; i < n; i++)
		cin >> ar[i];
	
	
	for (int i = 1; i < n; i++) {

	}
}

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int t = 1, i = 1;
	//cin >> t;
	while(t--) {
		//cout << "Case #" << i << ": ";
		solve();
		//i++;
	}
	return 0;
}


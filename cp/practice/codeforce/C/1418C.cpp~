#include <bits/stdc++.h>

using namespace std;

#define ll long long

void solve() {
	int a, n;
	cin >> n;
	int xdd[n];
	int dp[n][2];
	for (int i = 0; i < n; i++) {
		cin >> xdd[i];
	}
	dp[0][0] = 0;
	
	for (int i = 0; i < n; i++) {
		
	}
}

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int t = 1, i = 1;
	cin >> t;
	while(t--) {
		//cout << "Case #" << i << ": ";
		solve();
		//i++;
	}
	return 0;
}


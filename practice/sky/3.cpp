#include <bits/stdc++.h>

using namespace std;

#define ll long long

int f(string a) {
	int mx = 0;
	int n = a.length();
	for (int i = 0; i < n; i++) {
		for (int j = 0; j < n; j++) {
			string s;
			for (int k = 
		}
	}
}

void solve() {
	string s;
	cin >> s;
	cout << f(s);w
}

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int t = 1, i = 1;
	//cin >> t;
	while(t--) {
		//cout << "Case #" << i << ": ";
		solve();
		//i++;
	}
	return 0;
}


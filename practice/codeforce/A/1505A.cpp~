#include <bits/stdc++.h>
using namespace std;

#define ll long long

void solve() {
	string s;
	while(getline(cin, s)) {
		if (s == "Is it rated?") {
			cout << "NO" << endl;
		}
	}
}

int main() {
	ios::sync_with_stdio(1);
	cin.tie(0);

	int t = 1, i = 1;
	//cin >> t;
	while(t--) {
		//cout << "Case #" << i << ": ";
		solve();
		//i++;
	}
	return 0;
}



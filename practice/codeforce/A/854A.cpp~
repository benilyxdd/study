#include <bits/stdc++.h>

using namespace std;

#define ll long long

void solve() {
	int n;
	cin >> n;
	if (n % 2) {
		cout << n / 2 << " " << n / 2 + 1 << "\n";
	} else {
		if ((n/2) % 2 == 0) {
			cout << n/2-1 << " " << n/2+1 << "\n";
		} else {
			cout << n/2-2 << " " << n/2+2 << "\n";
		}
	}
}

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int t = 1, i = 1;
	//cin >> t;
	while(t--) {
		//cout << "Case #" << i < ": ";
		solve();
		//i++;
	}
	return 0;
}

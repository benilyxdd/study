#include <bits/stdc++.h>
using namespace std;

#define ll long long
const int mxN = 4001;
int ar[mxN], n, k;

int f(int as, int bs, int now) {
	if (as >= k && bs >= k)
		return now;

	if (now > n) 
		return -1;
	
	int ans = 0;
	for (int i = 0; i < n; i++) {
		ans = min(ans, f(
	}
}

void solve() {
	cin >> n >> k;
	for (int i = 0; i < n; i++)
		cin >> ar[i];
	
	
}

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int t = 1, i = 1;
	cin >> t;
	while(t--) {
		//cout << "Case #" << i << ": ";
		solve();
		//i++;
	}
	return 0;
}


#include <bits/stdc++.h>
using namespace std;

#define ll long long
const int mxN = 1001;
int a, n, h, s;

void solve() {
	cin >> n >> h;
	for (int i = 0; i < n; i++) {
		cin >> a;
		s += (a > h ? 2 : 1);
	}
	cout << s;
}

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int t = 1, i = 1;
	//cin >> t;
	while(t--) {
		//cout << "Case #" << i << ": ";
		solve();
		//i++;
	}
	return 0;
}



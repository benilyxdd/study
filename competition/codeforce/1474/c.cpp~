#include <bits/stdc++.h>
using namespace std;

#define ll long long
const int mxN = (int)2002;
int ar[mxN], n;
set<pair<int, int>> s;

void solve() {
	cin >> n;
	for (int i = 0; i < 2*n; i++) {
		cin >> ar[i];
		s.insert({ar[i], i});
	}
	


}

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int t = 1, i = 1;
	cin >> t;
	while(t--) {
		//cout << "Case #" << i << ": ";
		solve();
		//i++;
	}
	return 0;
}


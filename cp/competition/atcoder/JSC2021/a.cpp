#include <bits/stdc++.h>
using namespace std;

#define ll long long

int lcm(int a, int b) {
	return (a * b) / __gcd(a, b);
}

void solve() {
	int x, y, z;
	cin >> x >> y >> z;


}

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int t = 1, i = 1;
	//cin >> t;
	while(t--) {
		//cout << "Case #" << i << ": ";
		solve();
		//i++;
	}
	return 0;
}

